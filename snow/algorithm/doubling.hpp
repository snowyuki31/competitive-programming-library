#pragma once
#include <vector>

namespace snow {

/**
 * @brief Doubling
 */
template < typename T >
struct doubling {
    public:
        
    
    private:
        const int sz;
        std::vector<std::vector<T>> table;
};

} // namespace snow